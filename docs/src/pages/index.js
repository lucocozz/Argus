import Layout from '@theme/Layout';
import Hero from '@site/src/components/Hero';

export default function Home() {
  return (
    <Layout description="Modern C library for command-line argument parsing">
      <Hero />
      <main>{}</main>
    </Layout>
  );
}
